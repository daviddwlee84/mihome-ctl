"""Interactive terminal UI for mihome-ctl (``mihome-ctl tui``).

This subpackage requires the ``[tui]`` extra (Textual). Nothing here is imported
unless the user runs the ``tui`` subcommand, so the base install stays Textual-free.
"""
